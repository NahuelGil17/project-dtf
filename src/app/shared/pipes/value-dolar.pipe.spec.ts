import { ValueDolarPipe } from './value-dolar.pipe';

describe('ValueDolarPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueDolarPipe();
    expect(pipe).toBeTruthy();
  });
});
