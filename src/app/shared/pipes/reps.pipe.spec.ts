/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RepsPipe } from './reps.pipe';

describe('Pipe: Repse', () => {
  it('create an instance', () => {
    let pipe = new RepsPipe();
    expect(pipe).toBeTruthy();
  });
});
